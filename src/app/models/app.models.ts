export interface Project {
    name: string;
    link?: string;
    images: string[];
    techstack: string[];
    description: string;
}

export interface Skill {
    label: string;
    arr: string[];
}